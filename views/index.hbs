<!-- Sidebar -->
<aside class="bg-gray-800 text-white w-64 h-screen fixed">
  <!-- Sidebar Content -->
  <nav class="p-4">
    <ul class="space-y-4">
      <li><a href="#" class="block sidebar-link" data-form="university">University</a></li>
      <li><a href="#" class="block sidebar-link" data-form="course">Course</a></li>
      <li><a href="#" class="block sidebar-link" data-form="semester">Semester</a></li>
      <li><a href="#" class="block sidebar-link" data-form="subject">Subject</a></li>
      <li><a href="#" class="block sidebar-link" data-form="category">Catagory</a></li>
      <li><a href="#" class="block sidebar-link" data-form="module">Module</a></li>
      <li><a href="#" class="block sidebar-link" data-form="community">Community</a></li>
      <li><a href="#" class="block sidebar-link" data-form="stream">Stream</a></li>
      <li><a href="#" class="block sidebar-link" data-form="users">Users</a></li>
      <li><a href="/addvideo" class="block">Video</a></li>
      <li><a href="/adddoc" class="block">Doc</a></li>
    </ul>
  </nav>
</aside>

<!-- Main Content -->
<main class="ml-64 p-8">
  {{!-- <h1 class="text-2xl font-bold mb-4">Dashboard</h1> --}}

  <!-- University Form -->

  <div id="university-form" class="tab hidden">
    <form action="/university" method="post">

      <h2 class="text-xl font-semibold mb-2">Add University</h2>
      {{!-- {{user.user.name}} --}}
      <div class="mb-4">
        <label for="university-name" class="block">University Name:</label>
        <input name="uniname" type="text" id="university-name" class="border border-gray-400 px-4 py-2 rounded w-64">
      </div>
      <div class="mb-4">
        <label for="course-name" class="block">University Image:</label>
        <input name="image" type="text" id="course-name" class="border border-gray-400 px-4 py-2 rounded w-64">
      </div>
      <button type="submit" class="bg-blue-500 text-white px-4 py-2 rounded">Add</button>
    </form>
    <table class="min-w-full divide-y divide-gray-200 mt-12">
      <thead class="bg-gray-50">
        <tr>
          <th scope="col" class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">No
          </th>
          <th scope="col" class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">
            University Name</th>
          <th scope="col" class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">
            Logo</th>
          <th scope="col" class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">
            Actions</th>
        </tr>
      </thead>
      <tbody class="bg-white divide-y divide-gray-200">
        {{#each university}}
        <tr>
          <td class="px-6 py-4 whitespace-nowrap">{{@index}}</td>
          <td class="px-6 py-4 whitespace-nowrap">{{this.uniname}}</td>
          <td class="px-6 py-4 whitespace-nowrap"><img src="{{this.image}}" width="40px" alt=""></td>
          <td class="px-6 py-4 whitespace-nowrap">
            <a href="/university/delete/{{this._id}}" class="text-red-500 hover:text-red-700">Delete</a>
          </td>
        </tr>
        {{/each}}
        <!-- Add more table rows as needed -->
      </tbody>
    </table>
  </div>


  <!-- Course Form -->
  <div id="course-form" class="tab hidden">

    <form action="/course" method="post">
      <h2 class="text-xl font-semibold mb-2">Add Course</h2>
      <div class="mb-4">
        <label for="course-name" class="block">Course Name:</label>
        <input name="coursename" type="text" id="course-name" class="border border-gray-400 px-4 py-2 rounded w-64">
      </div>
      <div class="mb-4">
        <label for="course-name" class="block">Select Stream:</label>
        <select name="stream" id="stream" class="border border-gray-400 px-4 py-2 rounded w-64">
          {{#each stream}}
          <option value="{{this.fstreamname}}">{{this.streamname}}</option>
          {{/each}}
        </select>
      </div>
      <div class="mb-4">
        <label for="course-name" class="block">Course Image:</label>
        <input name="image" type="text" id="course-name" class="border border-gray-400 px-4 py-2 rounded w-64">
      </div>
      <button type="submit" class="bg-blue-500 text-white px-4 py-2 rounded">Add</button>
    </form>

    <table class="min-w-full divide-y divide-gray-200 mt-12">
      <thead class="bg-gray-50">
        <tr>
          <th scope="col" class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">No
          </th>
          <th scope="col" class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">
            Course Name</th>
          <th scope="col" class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">
            Stream</th>
          <th scope="col" class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">
            Logo</th>
          <th scope="col" class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">
            Actions</th>
        </tr>
      </thead>
      <tbody class="bg-white divide-y divide-gray-200">
        {{#each course}}
        <tr>
          <td class="px-6 py-4 whitespace-nowrap">{{@index}}</td>
          <td class="px-6 py-4 whitespace-nowrap">{{this.coursename}}</td>
          <td class="px-6 py-4 whitespace-nowrap">{{this.stream}}</td>
          <td class="px-6 py-4 whitespace-nowrap"><img src="{{this.image}}" alt="" width="40px"></td>
          <td class="px-6 py-4 whitespace-nowrap">
            <a href="/course/edit/{{this._id}}" class="text-blue-500 hover:text-red-700">Edit</a>
          </td>
          <td class="px-6 py-4 whitespace-nowrap">
            <a href="/course/delete/{{this._id}}" class="text-red-500 hover:text-red-700">Delete</a>
          </td>
        </tr>
        {{/each}}
        <!-- Add more table rows as needed -->
      </tbody>
    </table>



  </div>


  <!-- Community Form -->
  <div id="community-form" class="tab hidden">

    <form action="/community" method="post">
      <h2 class="text-xl font-semibold mb-2">Add Community</h2>
      <div class="mb-4">
        <label for="course-name" class="block">Community Name:</label>
        <input name="communityname" type="text" id="course-name" class="border border-gray-400 px-4 py-2 rounded w-64">
      </div>
      <div class="mb-4">
        <label for="course-name" class="block">Community Image Link:</label>
        <input name="image" type="text" id="course-name" class="border border-gray-400 px-4 py-2 rounded w-64">
      </div>
      <button type="submit" class="bg-blue-500 text-white px-4 py-2 rounded">Add</button>
    </form>

    <table class="min-w-full divide-y divide-gray-200 mt-12">
      <thead class="bg-gray-50">
        <tr>
          <th scope="col" class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">No
          </th>
          <th scope="col" class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">
            Community Name</th>
          <th scope="col" class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">
            Actions</th>
        </tr>
      </thead>
      <tbody class="bg-white divide-y divide-gray-200">
        {{#each community}}
        <tr>
          <td class="px-6 py-4 whitespace-nowrap">{{@index}}</td>
          <td class="px-6 py-4 whitespace-nowrap">{{this.communityname}}</td>
          <td class="px-6 py-4 whitespace-nowrap">
            <a href="/community/delete/{{this._id}}" class="text-red-500 hover:text-red-700">Delete</a>
          </td>
        </tr>
        {{/each}}
        <!-- Add more table rows as needed -->
      </tbody>
    </table>



  </div>


  <!-- Community Form -->
  <div id="stream-form" class="tab hidden">

    <form action="/stream" method="post">
      <h2 class="text-xl font-semibold mb-2">Add Stream</h2>
      <div class="mb-4">
        <label for="course-name" class="block">Stream Name:</label>
        <input name="streamname" type="text" id="course-name" class="border border-gray-400 px-4 py-2 rounded w-64">
      </div>
      <button type="submit" class="bg-blue-500 text-white px-4 py-2 rounded">Add</button>
    </form>

    <table class="min-w-full divide-y divide-gray-200 mt-12">
      <thead class="bg-gray-50">
        <tr>
          <th scope="col" class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">No
          </th>
          <th scope="col" class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">
            Stream Name</th>
          <th scope="col" class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">
            Actions</th>
        </tr>
      </thead>
      <tbody class="bg-white divide-y divide-gray-200">
        {{#each stream}}
        <tr>
          <td class="px-6 py-4 whitespace-nowrap">{{@index}}</td>
          <td class="px-6 py-4 whitespace-nowrap">{{this.streamname}}</td>
          <td class="px-6 py-4 whitespace-nowrap">
            <a href="/stream/delete/{{this._id}}" class="text-red-500 hover:text-red-700">Delete</a>
          </td>
        </tr>
        {{/each}}
        <!-- Add more table rows as needed -->
      </tbody>
    </table>



  </div>


  <!-- Users Form -->
  <div id="users-form" class="tab hidden">
    <table class="min-w-full divide-y divide-gray-200 mt-12">
      <thead class="bg-gray-50">
        <tr>
          <th scope="col" class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">No
          </th>
          <th scope="col" class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">
            Users Name</th>
          <th scope="col" class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">
            Users Email</th>
          <th scope="col" class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">
            Phone</th>
          <th scope="col" class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">
            Actions</th>
        </tr>
      </thead>
      <tbody class="bg-white divide-y divide-gray-200">
        {{#each users}}
        <tr>
          <td class="px-6 py-4 whitespace-nowrap">{{@index}}</td>
          <td class="px-6 py-4 whitespace-nowrap">{{this.name}}</td>
          <td class="px-6 py-4 whitespace-nowrap">{{this.gmail}}</td>
          <td class="px-6 py-4 whitespace-nowrap">{{this.phone}}</td>
          <td class="px-6 py-4 whitespace-nowrap">
            <a href="/users/delete/{{this._id}}" class="text-red-500 hover:text-red-700">Delete</a>
          </td>
        </tr>
        {{/each}}
        <!-- Add more table rows as needed -->
      </tbody>
    </table>



  </div>



  <!-- Semester Form -->
  <div id="semester-form" class="tab hidden">
    <form action="/semester" method="post">

      <h2 class="text-xl font-semibold mb-2">Add Semester</h2>
      <div class="mb-4">
        <label for="semester-name" class="block">Semester Name:</label>
        <input name="semestername" type="text" id="semester-name" class="border border-gray-400 px-4 py-2 rounded w-64">
      </div>
      <button type="submit" class="bg-blue-500 text-white px-4 py-2 rounded">Add</button>
    </form>
    <table class="min-w-full divide-y divide-gray-200 mt-12">
      <thead class="bg-gray-50">
        <tr>
          <th scope="col" class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">No
          </th>
          <th scope="col" class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">
            Semester Name</th>
          <th scope="col" class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">
            Actions</th>
        </tr>
      </thead>
      <tbody class="bg-white divide-y divide-gray-200">
        {{#each semester}}
        <tr>
          <td class="px-6 py-4 whitespace-nowrap">{{@index}}</td>
          <td class="px-6 py-4 whitespace-nowrap">{{this.semestername}}</td>
          <td class="px-6 py-4 whitespace-nowrap">
            <a href="/semester/delete/{{this._id}}" class="text-red-500 hover:text-red-700">Delete</a>
          </td>
        </tr>
        {{/each}}
        <!-- Add more table rows as needed -->
      </tbody>
    </table>

  </div>


  <!-- Subject Form -->
  <div id="subject-form" class="tab hidden">
    <form action="/subject" method="post">

      <h2 class="text-xl font-semibold mb-2">Add Subject</h2>
      <div class="mb-4">
        <label for="university-select" class="block">University:</label>
        <select name="universityname" id="university-select" class="border border-gray-400 px-4 py-2 rounded w-64">
          {{#each university}}
          <option value="{{this.funiname}}">{{this.uniname}}</option>
          {{/each}}
        </select>
      </div>
      <div class="mb-4">
        <label for="course-select" class="block">Course:</label>
        <select name="coursename" id="course-select" class="border border-gray-400 px-4 py-2 rounded w-64">
          {{#each course}}
          <option value="{{this.fcoursename}}">{{this.coursename}}</option>
          {{/each}}
        </select>
      </div>
      <div class="mb-4">
        <label for="semester-select" class="block">Semester:</label>
        <select name="semestername" id="semester-select" class="border border-gray-400 px-4 py-2 rounded w-64">
          {{#each semester}}
          <option value="{{this.fsemestername}}">{{this.semestername}}</option>
          {{/each}}
        </select>
      </div>
      <div class="mb-4">
        <label for="subject-name" class="block">Subject Name:</label>
        <input name="subjectname" type="text" id="subject-name" class="border border-gray-400 px-4 py-2 rounded w-64">
      </div>
      <button type="submit" class="bg-blue-500 text-white px-4 py-2 rounded">Add</button>
    </form>

    <table class="min-w-full divide-y divide-gray-200 mt-12">
      <thead class="bg-gray-50">
        <tr>
          <th scope="col" class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">No
          </th>
          <th scope="col" class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">
            Subject</th>
          <th scope="col" class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">
            University</th>
          <th scope="col" class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">
            Course</th>
          <th scope="col" class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">
            Semester</th>
          <th scope="col" class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">
            Actions</th>
        </tr>
      </thead>
      <tbody class="bg-white divide-y divide-gray-200">
        {{#each subject}}
        <tr>
          <td class="px-6 py-4 whitespace-nowrap">{{@index}}</td>
          <td class="px-6 py-4 whitespace-nowrap">{{this.subjectname}}</td>
          <td class="px-6 py-4 whitespace-nowrap">{{this.universityname}}</td>
          <td class="px-6 py-4 whitespace-nowrap">{{this.coursename}}</td>
          <td class="px-6 py-4 whitespace-nowrap">{{this.semestername}}</td>
          <td class="px-6 py-4 whitespace-nowrap">
            <a href="/subject/delete/{{this._id}}" class="text-red-500 hover:text-red-700">Delete</a>
          </td>
        </tr>
        {{/each}}
        <!-- Add more table rows as needed -->
      </tbody>
    </table>


  </div>


  <!-- Category Form -->
  <div id="category-form" class="tab hidden">
    <form action="/category" method="post">

      <h2 class="text-xl font-semibold mb-2">Add Category</h2>
      <div class="mb-4">
        <label for="category-name" class="block">Category Name:</label>
        <input name="categoryname" type="text" id="category-name" class="border border-gray-400 px-4 py-2 rounded w-64">
      </div>
      <button type="submit" class="bg-blue-500 text-white px-4 py-2 rounded">Add</button>
    </form>
    <table class="min-w-full divide-y divide-gray-200 mt-12">
      <thead class="bg-gray-50">
        <tr>
          <th scope="col" class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">No
          </th>
          <th scope="col" class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">
            Category Name</th>
          <th scope="col" class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">
            Actions</th>
        </tr>
      </thead>
      <tbody class="bg-white divide-y divide-gray-200">
        {{#each category}}
        <tr>
          <td class="px-6 py-4 whitespace-nowrap">{{@index}}</td>
          <td class="px-6 py-4 whitespace-nowrap">{{this.categoryname}}</td>
          <td class="px-6 py-4 whitespace-nowrap">
            <a href="/category/delete/{{this._id}}" class="text-red-500 hover:text-red-700">Delete</a>
          </td>
        </tr>
        {{/each}}
        <!-- Add more table rows as needed -->
      </tbody>
    </table>

  </div>

  {{!-- Module Form --}}
  <div id="module-form" class="tab hidden">
    <h2 class="text-xl font-semibold mb-2">Add Module</h2>

    <form action="/module" method="post">
      {{!-- <div class="mb-4">
        <label for="university-select" class="block">University:</label>
        <select name="universityname" id="university-select" class="border border-gray-400 px-4 py-2 rounded w-64">
          {{#each university}}
          <option value="{{this.funiname}}">{{this.uniname}}</option>
          {{/each}}
        </select>
      </div>
      <div class="mb-4">
        <label for="course-select" class="block">Course:</label>
        <select name="coursename" id="course-select" class="border border-gray-400 px-4 py-2 rounded w-64">
          {{#each course}}
          <option value="{{this.fcoursename}}">{{this.coursename}}</option>
          {{/each}}
        </select>
      </div>
      <div class="mb-4">
        <label for="semester-select" class="block">Semester:</label>
        <select name="semestername" id="semester-select" class="border border-gray-400 px-4 py-2 rounded w-64">
          {{#each semester}}
          <option value="{{this.fsemestername}}">{{this.semestername}}</option>
          {{/each}}
        </select>
      </div> --}}

      {{!-- <div class="mb-4">
        <label for="subject-select" class="block">Subject:</label>
        <select name="subjectname" id="subject-select" class="border border-gray-400 px-4 py-2 rounded w-64">
          {{#each subject}}
          <option value="{{this.fsubjectname}}">{{this.subjectname}}</option>
          {{/each}}
        </select>
      </div> --}}

      <div class="mb-4">
        <label for="module" class="block">Module Name</label>
        <input type="text" id="module" name="modulename" class="border border-gray-400 px-4 py-2 rounded w-64">
      </div>

      <button type="submit" class="bg-blue-500 text-white py-2 px-4 rounded">Submit</button>
    </form>

    <table class="min-w-full divide-y divide-gray-200 mt-12">
      <thead class="bg-gray-50">
        <tr>
          <th scope="col" class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">No
          </th>
          <th scope="col" class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">
            Module</th>
          <th scope="col" class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">
            University</th>
          <th scope="col" class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">
            Course</th>
          <th scope="col" class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">
            Semester</th>
          <th scope="col" class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">
            Subject</th>
          <th scope="col" class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">
            Actions</th>
        </tr>
      </thead>
      <tbody class="bg-white divide-y divide-gray-200">
        {{#each module}}
        <tr>
          <td class="px-6 py-4 whitespace-nowrap">{{@index}}</td>
          <td class="px-6 py-4 whitespace-nowrap">{{this.modulename}}</td>
          <td class="px-6 py-4 whitespace-nowrap">{{this.universityname}}</td>
          <td class="px-6 py-4 whitespace-nowrap">{{this.coursename}}</td>
          <td class="px-6 py-4 whitespace-nowrap">{{this.semestername}}</td>
          <td class="px-6 py-4 whitespace-nowrap">{{this.subjectname}}</td>
          <td class="px-6 py-4 whitespace-nowrap">
            <a href="/module/delete/{{this._id}}" class="text-red-500 hover:text-red-700">Delete</a>
          </td>
        </tr>
        {{/each}}
        <!-- Add more table rows as needed -->
      </tbody>
    </table>
  </div>
</main>

<script>
  // Get the sidebar links
  const sidebarLinks = document.querySelectorAll('.sidebar-link');
  // Get the forms
  const forms = document.querySelectorAll('.tab');

  // Add click event listener to each sidebar link
  sidebarLinks.forEach(link => {
    link.addEventListener('click', e => {
      e.preventDefault();
      const formId = link.dataset.form;

      // Hide all forms
      forms.forEach(form => {
        form.classList.add('hidden');
      });

      // Show the selected form
      const selectedForm = document.getElementById(`${formId}-form`);
      selectedForm.classList.remove('hidden');
      // Update the active state of the sidebar link
      sidebarLinks.forEach(link => {
        link.classList.remove('text-blue-500');
      });
      link.classList.add('text-blue-500');
    });
  });

  window.onload = function () {
    var fragment = window.location.hash.substring(1);
    console.log(fragment);
    if (fragment) {
      document.getElementById(fragment).classList.remove('hidden');
    } else {
      document.getElementById('university-form').classList.remove('hidden');
    }

  };

</script>